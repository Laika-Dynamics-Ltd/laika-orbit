name: release 1brain
# Publishes the `1brain` npm package when a tag like 1brain-v0.1.1 is pushed. The tag must match
# packages/1brain/package.json's version. npm trusted publishing (OIDC) authenticates the run and
# attaches provenance, so there is no npm token in the repo's secrets.
on:
  push:
    tags: ['1brain-v*']

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: pnpm, registry-url: 'https://registry.npmjs.org' }
      - run: npm install -g npm@latest # trusted publishing needs npm 11.5.1 or newer
      - run: pnpm install --frozen-lockfile

      - name: tag matches the package version
        run: |
          want="1brain-v$(node -p "require('./packages/1brain/package.json').version")"
          test "$GITHUB_REF_NAME" = "$want" || { echo "tag $GITHUB_REF_NAME, package says $want"; exit 1; }

      - name: smoke
        run: pnpm --filter 1brain smoke

      # pnpm pack writes the manifest npm users see (no workspace: specifiers); npm publishes it
      - name: publish
        working-directory: packages/1brain
        run: |
          pnpm pack
          npm publish ./1brain-*.tgz --provenance --access public
