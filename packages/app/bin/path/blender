#!/bin/sh
# Blender background renders, offloaded to the least busy Laika Orbit machine (see ../../blender-offload.mjs).
# Put this folder first on PATH:  PATH="/path/to/packages/app/bin/path:$PATH"
exec node "$(cd "$(dirname "$0")" && pwd)/../../blender-offload.mjs" "$@"
